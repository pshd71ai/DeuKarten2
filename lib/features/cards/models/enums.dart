enum DifficultyLevel { a1, a2, b1, b2, c1 }

enum CardType { noun, verb, adjective, article, sentence }

enum SessionStatus { inProgress, completed, paused }

enum CardStatus { new_card, learning, review, mastered }
